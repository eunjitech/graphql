# 어떤 query나 mutation을 가졌는지 알려줌
# 데이터의 형식을 정의

type Movie {
  id: Int!
  title: String!
  rating: Float!
  language: String!
  summary: String!
  medium_cover_image: String!
}

type Query {
  movies(limit: Float, rating: Float): [Movie]
}
